{
  "name": "blog",
  "type": "module",
  "devDependencies": {
    "jstransformer-markdown-it": "^3.0.0",
    "pug-cli": "^1.0.0-alpha6"
  },
  "trustedDependencies": [
    "core-js"
  ],
  "scripts": {
    "build": "pug -O options.json src/index.pug src/blog/*.pug -o dist -D"
  }
}